import { LoginData } from "models/userLogin";
import { UserRegistration } from "models/userRegister";


class UserAuth {
    
    // register
    register(userDetails: UserRegistration) {

    }

    // login
    login(userDetails: LoginData) {
        
    }

    // auth
}